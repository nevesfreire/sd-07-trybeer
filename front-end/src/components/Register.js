import React from 'react';

const ComponentRegister = () => {
  const title = 'Register'; // coloquei esse title para corrigir um erro de eslint, pode remover
  return (
    <div>
      <h1>{ title }</h1>
    </div>
  );
};

export default ComponentRegister;
